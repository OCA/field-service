<odoo>
    <!-- Operations -->
    <record id="fsm_order_form" model="ir.ui.view">
        <field name="name">fsm.order.form</field>
        <field name="model">fsm.order</field>
        <field name="inherit_id" ref="fieldservice.fsm_order_form" />
        <field name="arch" type="xml">
            <button name="action_complete" position="before">
                <button
                    id="action_confirm"
                    name="action_confirm"
                    string="Confirm"
                    class="oe_highlight"
                    type="object"
                    groups="fieldservice.group_fsm_dispatcher"
                    invisible="stage_id.name != 'New'"
                />
                <button
                    id="action_request"
                    name="action_request"
                    string="Request"
                    class="oe_highlight"
                    type="object"
                    groups="fieldservice.group_fsm_dispatcher"
                    invisible="stage_id.name not in ['Confirmed', 'Requested']"
                />
                <button
                    id="action_assign"
                    name="action_assign"
                    string="Assign"
                    class="oe_highlight"
                    type="object"
                    groups="fieldservice.group_fsm_dispatcher"
                    invisible="stage_id.name not in ['Confirmed', 'Requested']"
                />
                <button
                    id="action_schedule"
                    name="action_schedule"
                    string="Schedule"
                    class="oe_highlight"
                    type="object"
                    groups="fieldservice.group_fsm_dispatcher"
                    invisible="stage_id.name not in ['Confirmed', 'Requested', 'Assigned']"
                />
                <button
                    id="action_enroute"
                    name="action_enroute"
                    string="En Route"
                    class="oe_highlight"
                    type="object"
                    groups="fieldservice.group_fsm_dispatcher"
                    invisible="stage_id.name != 'Scheduled'"
                />
                <button
                    id="action_start"
                    name="action_start"
                    string="Start"
                    class="oe_highlight"
                    type="object"
                    groups="fieldservice.group_fsm_dispatcher"
                    invisible="stage_id.name not in ['Scheduled', 'En Route']"
                />
            </button>
        </field>
    </record>

    <record id="fsm_order_list_view" model="ir.ui.view">
        <field name="name">fsm.order.list</field>
        <field name="model">fsm.order</field>
        <field name="inherit_id" ref="fieldservice.fsm_order_list_view" />
        <field name="arch" type="xml">
            <list name="orders" position="attributes">
                <attribute name="decoration-danger">stage_id.name in [
                    'New', 'Confirmed', 'Requested', 'Assigned']</attribute>

                <attribute name="decoration-info">stage_id.name in [
                    'Scheduled', 'En Route', 'Started', 'Completed']</attribute>
            </list>
        </field>
    </record>

    <!-- Search for fsm.order-->
    <record id="fsm_order_search_view" model="ir.ui.view">
        <field name="name">fsm.order.search</field>
        <field name="model">fsm.order</field>
        <field name="inherit_id" ref="fieldservice.fsm_order_search_view" />
        <field name="arch" type="xml">
            <search string="Orders">
                <filter name="order_today" position="before">
                    <filter
                        string="Past Due Orders"
                        domain="[('stage_id.name', 'not in', ['Started', 'Completed', 'Cancelled']), ('scheduled_date_start', '&lt;', current_date)]"
                        name="past_due"
                    />
                </filter>
            </search>
        </field>
    </record>
</odoo>
