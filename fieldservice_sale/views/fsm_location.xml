<odoo>
    <record id="fsm_location_sale_order_action" model="ir.actions.act_window">
        <field name="name">Quotations and Sales</field>
        <field name="res_model">sale.order</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form,graph</field>
        <field name="context">{'search_default_fsm_location_id': active_id}</field>
        <field name="groups_id" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new quotation, the first step of a new sale!
            </p><p>
                Once the quotation is confirmed by the customer, it becomes a sales order.<br/> You will be able to create an invoice and collect the payment.
            </p>
        </field>
    </record>

    <!-- Field Service Location Form View-->
    <record id="fsm_location_form_view" model="ir.ui.view">
        <field name="name">fsm.location.form</field>
        <field name="model">fsm.location</field>
        <field name="inherit_id" ref="fieldservice.fsm_location_form_view"/>
        <field name="arch" type="xml">
            <page id="others-page" position="before">
                <page string="Sales">
                    <group>
                        <group>
                            <field name="sales_territory_id"/>
                            <field name="user_id"/>
                        </group>
                    </group>
                </page>
            </page>
            <div class="oe_button_box" position="inside">
                <button class="oe_stat_button" type="action" name="%(fieldservice_sale.fsm_location_sale_order_action)d" groups="sales_team.group_sale_salesman" attrs="{'invisible': [('sale_order_count', '=', 0)]}" icon="fa-usd">
                    <field string="Sales" name="sale_order_count" widget="statinfo"/>
                </button>
            </div>
        </field>
    </record>
</odoo>
