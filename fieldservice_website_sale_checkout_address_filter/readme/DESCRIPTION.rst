This module filters available shipping addresses at checkout, showing only those linked to valid Field Service locations with active and correctly configured routes.

If no valid shipping addresses are available, the checkout page displays a warning message and prompts the user to contact support using a contact form. Additionally, if shipping address details are incomplete, a clear alert is shown with instructions to request assistance.
